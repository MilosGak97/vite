<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Project</title>
    <link href="output.css" rel="stylesheet">
    <link href="styles.css" rel="stylesheet">
</head>

<body class="bg-third">
    <%- include('partials/menu') %>

        <div class="container mx-auto p-4">
            <h1 class="text-2xl font-bold mb-4">Shipping List</h1>

            <table class="text-center min-w-full bg-white border">
                <thead>
                    <tr>
                        <th class="py-2 px-4 border-b">ID</th>
                        <th class="py-2 px-4 border-b">Creation Date</th>
                        <th class="py-2 px-4 border-b">Shipping Date</th>
                        <th class="py-2 px-4 border-b">Total Properties</th>
                    </tr>
                </thead>
                <tbody>
                    <% shippings.forEach(shipping=> { %>
                        <tr>
                            <td class="py-2 px-4 border-b">
                                <a href="/shipping/<%= shipping._id %>" class="text-blue-500 hover:underline">
                                    <%= shipping._id %>
                                </a>
                            </td>
                            <td class="py-2 px-4 border-b">
                                <%= shipping.created_at ? new Date(shipping.created_at).toLocaleString() : 'Not Set' %>
                            </td>
                            <td class="py-2 px-4 border-b">
                                <%= shipping.shipping_date ? new Date(shipping.shipping_date).toLocaleDateString()
                                    : 'Not Set' %>
                            </td>
                            <td class="py-2 px-4 border-b">
                                <%= shipping.total_count %>
                            </td>
                        </tr>
                        <% }) %>
                </tbody>
            </table>
        </div>
</body>

</html>